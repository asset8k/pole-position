[project]
name = "pole-position"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Asset", email = "asset8k@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pole-position = "pole_position:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
